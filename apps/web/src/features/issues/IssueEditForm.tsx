import { FormEvent, useState } from 'react';
import type { Issue } from '../../lib/api';
import { api } from '../../lib/api';
export function IssueEditForm({ issue, onSaved }: { issue: Issue; onSaved: () => Promise<void> }) {
  const [title, setTitle] = useState(issue.title); const [body, setBody] = useState(issue.body ?? ''); const [assignee, setAssignee] = useState(issue.assignee ?? ''); const [labels, setLabels] = useState(issue.labels.map((label) => label.name).join(', ')); const [milestone, setMilestone] = useState(issue.milestone ?? ''); const [state, setState] = useState(issue.state); const [error, setError] = useState<string>(); const [saving, setSaving] = useState(false);
  const submit = async (event: FormEvent) => { event.preventDefault(); if (!title.trim()) return setError('Title 不可為空白'); setSaving(true); setError(undefined); try { await api(`/api/issues/${issue.owner}/${issue.name}/${issue.number}`, { method: 'PATCH', body: JSON.stringify({ title: title.trim(), body, state, assignee: assignee || null, labels: labels.split(',').map((item) => item.trim()).filter(Boolean), milestone: milestone || null }) }); await onSaved(); } catch (cause) { setError(cause instanceof Error ? cause.message : '更新 Issue 失敗'); } finally { setSaving(false); } };
  return <form className="edit-form" onSubmit={submit}><h2>編輯 Issue</h2><input aria-label="Title" value={title} onChange={(e) => setTitle(e.target.value)} /><textarea aria-label="Description" value={body} onChange={(e) => setBody(e.target.value)} /><select aria-label="State" value={state} onChange={(e) => setState(e.target.value as Issue['state'])}><option value="open">Open</option><option value="closed">Closed</option></select><input aria-label="Assignee" placeholder="Assignee login" value={assignee} onChange={(e) => setAssignee(e.target.value)} /><input aria-label="Labels" placeholder="label-a, label-b" value={labels} onChange={(e) => setLabels(e.target.value)} /><input aria-label="Milestone" placeholder="Milestone" value={milestone} onChange={(e) => setMilestone(e.target.value)} /><button disabled={saving} type="submit">{saving ? '儲存中…' : '儲存變更'}</button>{error && <div className="error" role="alert">{error}</div>}</form>;
}
