export function BoardRepositoryPicker({ repositories }: { repositories: Array<{ owner: string; name: string; conventionId?: string | null; conventionVersion?: string | null }> }) { return <ul>{repositories.map((repository) => <li key={`${repository.owner}/${repository.name}`}>{repository.owner}/{repository.name} {repository.conventionId ? `(${repository.conventionId}@${repository.conventionVersion})` : '(未設定 Convention)'}</li>)}</ul>; }
