import type { Issue } from '../../lib/api';
export function KanbanCard({ issue, onDragStart }: { issue: Issue; onDragStart: (issue: Issue) => void }) { return <article className="kanban-card" draggable onDragStart={() => onDragStart(issue)}><a href={`/issue/${issue.owner}/${issue.name}/${issue.number}`}>{issue.title}</a><small>{issue.owner}/{issue.name} #{issue.number}</small><small>{issue.assignee ?? '未指派'}</small><div className="labels">{issue.labels.map((label) => <span className="label" key={label.name}>{label.name}</span>)}</div></article>; }
